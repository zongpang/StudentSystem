package com.bc.stdsys.util;


public class Test {
	public static void main(String[] args) {
		// try {
		// Class c = Class.forName("com.bc.stdsys.entitys.Teacher");
		// boolean isExist = c.isAnnotationPresent(Table.class);
		// if (isExist) {
		// Table t = (Table) c.getAnnotation(Table.class);
		// System.out.println(t.value());
		//
		// // 找到字段上边的注解
		//
		// Field[] fields = c.getDeclaredFields();
		//
		// for (Field field : fields) {
		// boolean itHave = field.isAnnotationPresent(Column.class);
		// if (itHave) {
		// Column co = (Column) field.getAnnotation(Column.class);
		// System.out.println(co.value());
		// }
		// }
		//
		// }
		//
		// } catch (ClassNotFoundException e) {
		// // TODO Auto-generated catch block
		// e.printStackTrace();
		// }

		System.out.println((5%1)+"");

	}
}
